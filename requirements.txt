# Single source of truth: pyproject.toml. This file mirrors the `[project].dependencies`
# block for convenience (`pip install -r requirements.txt`).

# --- Core runtime ---
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
sse-starlette>=2.1.0
pydantic>=2.7.0
pydantic-settings>=2.4.0
python-dotenv>=1.0.1

# --- LLM stack (vLLM via OpenAI-compatible API) ---
openai>=1.40.0
langgraph>=0.2.0

# --- Storage ---
neo4j>=5.24.0
psycopg[binary]>=3.2.0
psycopg-pool>=3.2.0

# --- Resilience / observability ---
httpx>=0.27.0
tenacity>=8.5.0
structlog>=24.4.0
prometheus-client>=0.20.0

# --- Optional: vLLM server (heavy CUDA deps). install via:
#     pip install -e '.[vllm]'
# vllm>=0.6.0
